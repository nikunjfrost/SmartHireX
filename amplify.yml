version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install --no-audit
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: frontend/dist
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
